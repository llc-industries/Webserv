server {
    listen 8080;
    host localhost;

    root www/;
    error_page 404 error_pages/404.html;

      location / {
        allow_methods GET POST DELETE;
        autoindex y;
    }

      location *.py {
        cgi_pass /usr/bin/python;
      }
}
